# Loaded only under `--mode demo` (npm run demo / npm run demo:build).
# Forces the auth-free local demo (seeded browser data), ignoring any Supabase
# keys in .env.local. No secrets here — safe to commit.
VITE_DATA_MODE=local
